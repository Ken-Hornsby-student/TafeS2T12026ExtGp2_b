<Page
    x:Class="Calculator.MainMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Calculator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	
	<Page.Resources>

		<!-- Text Block Title -->
		<Style x:Key="title" TargetType="TextBlock">
			<Setter Property="FontFamily" Value="Arial"/>
			<Setter Property="FontSize" Value="18"/>
			<Setter Property="Foreground" Value="GreenYellow"/>
			<!--<Setter Property="FontStyle" Value="Bold"/>-->
			<Setter Property="Height" Value="40"/>
			<Setter Property="Padding" Value="20,10,5,0"/>
		</Style>

		<!--Button Composure-->
		<Style TargetType="Button" >
			<Setter Property="Background" Value="DarkCyan" />
			<Setter Property="FontFamily" Value="Arial" />
			<Setter Property="FontSize" Value="12" />
			<Setter Property="Padding" Value="5" />
			<Setter Property="Margin" Value="5" />
			<Setter Property="MinWidth" Value="80" />
			<Setter Property="BorderBrush" Value="Pink"/>
			<Setter Property="BorderThickness" Value="2"/>
			<Setter Property="CornerRadius" Value="5"/>
		</Style>

	</Page.Resources>


	<Grid>

		<!-- Outer Bourder Margin Stack Panel -->
		<StackPanel Orientation="Vertical" VerticalAlignment="Center" Background="Gray" Width="400" Height="250" BorderThickness="1" CornerRadius="5" BorderBrush="Gray" Padding="10" >


			<!-- Container Stack Panel -->
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Center" BorderThickness="5" CornerRadius="5" BorderBrush="Cyan" Height="220"  Width="370" Padding="10">

				<!-- Left Stack Panel with Title and Buttons -->
				<StackPanel Orientation="Vertical" VerticalAlignment="Center" Width="180" Height="190">

					<TextBlock Style="{StaticResource title}" Text="Universal Calculator"/>

					<!-- Yet to set up this button to click. Direct to MainPage file. -->
					<Button x:Name="mathCalc" Content="Math Calculator"  />

					<!-- Yet to set up this button to click. Direct to Calculator file. -->
					<Button x:Name="mortgageCalc" Content="Mortgage Calculator" />

					<!-- Yet to set up this button to click. Direct to Currency file. -->
					<Button x:Name="currencyCalc" Content="Currency Calculator" />

					<!-- Yet to set up this button to click. Engineer an exit strategy in this file. -->
					<Button x:Name="exit" Content="Exit" />

				</StackPanel>

				<!-- Right Top Stack Panel with picture -->
				<StackPanel Orientation="Vertical" VerticalAlignment="Top" Width="155"  Padding="5, 5, 5, 5">

					<Image x:Name="abacus" Source="/Pictures/abacus.jpg" Height="85" Margin="5" />

				</StackPanel>

			</StackPanel>

		</StackPanel>

	</Grid>
</Page>
